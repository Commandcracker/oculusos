local a=shell.resolve(".")local b={...}function listDir(c,d)if c==""then c="\\"end;for e,f in pairs(fs.list(c))do local g=c.."\\"..f;term.write(d.."-- ")if term.isColor()and fs.isDir(g)then term.setTextColour(colors.blue)end;print(f)term.setTextColour(colors.white)if fs.isDir(g)then listDir(g,d.."   |")end end end;if#b>0 then a=shell.resolve(b[1])end;if term.isColor()then term.setTextColour(colors.blue)else term.setTextColour(colors.white)end;print('/'..a)term.setTextColour(colors.white)listDir(a,"|")