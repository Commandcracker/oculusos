local a={...}if#a==0 then print("Usage: display <file>")return end;local b=shell.resolve(a[1])if fs.exists(b)and fs.isDir(b)then printError("Cannot display a directory.")return end;if fs.exists(b)then paintutils.drawImage(paintutils.loadImage(b),1,1)else printError("file not found")return end;while true do local c=os.pullEvent()if c=="mouse_click"or c=="key"or c=="paste"or c=="char"then term.clear()term.setCursorPos(1,1)return end end