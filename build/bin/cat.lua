local a={...}if#a==0 then print("Usage: cat <path>")return end;local b=shell.resolve(a[1])if fs.exists(b)and fs.isDir(b)then printError("Cannot cat a directory.")return end;if fs.exists(b)then local c=fs.open(b,"rb")print(c.readAll())c.close()else printError("File not found")return end