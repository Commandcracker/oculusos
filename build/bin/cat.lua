local a={...}if#a==0 then print("Usage: cat <path>")return end;local b=shell.resolve(a[1])local c=fs.isReadOnly(b)if fs.exists(b)and fs.isDir(b)then printError("Cannot cat a directory.")return end;if fs.exists(b)then local d=fs.open(b,"rb")print(d.readAll())d.close()else printError("File not found")return end