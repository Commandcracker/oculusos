local a={...}if#a==0 then print("Usage: cat <path>")return end;local b=shell.resolve(a[1])local c=fs.isReadOnly(b)if fs.exists(b)and fs.isDir(b)then printError("Cannot cat a directory.")return end;if fs.exists(b)then local d=io.open(b,"r")local e=d:read()print(e)while e do e=d:read()if e then print(e)end end;d:close()else printError("file not found")return end